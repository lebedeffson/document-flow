- extract the packages inside the lib/python/Products folder

- install the Python extensions:

      python2.1 setup.py install

- restart Zope

- check the permissions of the TextIndexNG2 folder (the user
  running zope must be able to access the folder)
